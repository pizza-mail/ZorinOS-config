In terminal navigate to:

cd ~/Downloads

Make the script executable:

chmod +x setup.sh

Run it:

./setup.sh

Do not run as SUDO

summer today voyage true shallow hover crop regret over door base wood tackle step mean panda when rely engage gauge shoot puzzle modify network fine rich
